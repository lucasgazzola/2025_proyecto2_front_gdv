export interface ProductDto {
  id: string;
  idEcommerce: string;
  name: string;
  quantity: number;
}

export interface ProductFormData {
  idEcommerce: string;
  name: string;
  quantity: number;
}
